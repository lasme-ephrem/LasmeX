# The `minimal` agent preset: a fixed-prompt, two-tool coding-agent composition.
#
# The persona is the complete system prompt, so global identity, Web orientation,
# tool guidance, and later assembly listeners cannot add prompt text. Runtime
# context snapshots are suppressed for this preset. The model composes the
# platform shell (`bash` with persistent state, or one-shot `pwsh` on Windows)
# and `str_replace_editor`. Context compaction is absent.

- id: persona
  name: 'lasmex-persona'
  config:
    text: You are a helpful software engineer assistant.
    complete: true
    includeRuntimeContext: false

# The PTY registry is an agent-owned service, so it lives in an entry-local
# realm. The backend still consumes the host sandbox policy and subprocess
# implementation, while the tool registers into this agent's scoped catalog.
- id: persistent-shell
  name: cordis:group
  group: true
  disabled: !!js process.platform === 'win32'
  isolate:
    terminals: true
  config:
    - id: pty
      name: 'lasmex-terminal'

    - id: terminal-bash
      name: 'lasmex-terminal-bash'
      config:
        timeoutMs: 300000

    - id: persistent-bash
      name: 'lasmex-tool-bash-persistent'
      config:
        timeoutMs: 300000
        description: |-
          Run commands in a bash shell
          * When invoking this tool, the contents of the "command" parameter does NOT need to be XML-escaped.
          * You don't have access to the internet via this tool.
          * You do have access to a mirror of common linux and python packages via apt and pip.
          * State is persistent across command calls and discussions with the user.
          * To inspect a particular line range of a file, e.g. lines 10-25, try 'sed -n 10,25p /path/to/the/file'.
          * Please avoid commands that may produce a very large amount of output.
          * Please run long lived commands in the background, e.g. 'sleep 10 &' or start a server in the background.

# The persistent terminal backend relies on POSIX process-group inspection.
# Windows uses the shipped PowerShell consumer over the host shell provider,
# matching every other built-in preset instead of exposing a tool that fails on
# its first call.
- id: tool-pwsh
  name: 'lasmex-tool-pwsh'
  disabled: !!js process.platform !== 'win32'

# The bare local filesystem shadows the host's sandboxed provider only for this
# preset. The editor shares that realm and requires absolute paths.
- id: filesystem
  name: cordis:group
  group: true
  isolate:
    fs: true
  config:
    - id: fs-local
      name: 'lasmex-fs-local'
      config:
        cwd: !!js process.env.LASMEX_CWD ?? process.cwd()

    - id: str-replace-editor
      name: 'lasmex-tool-str-replace-editor'
      config:
        maxOutputChars: 16000
