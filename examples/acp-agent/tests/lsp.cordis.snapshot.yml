# Keyless replay keeps the LSP composition intact and replaces only the model adapter.
- id: base
  name: '@deepseek-ai/cordis-plugin-include'
  config:
    path: ../cordis.yml
    patches:
      - id: llm-deepseek
        name: 'lasmex-llm-deepseek'
        disabled: true
      - insert:
          - id: lsp
            name: 'lasmex-lsp'
          - id: lsp-stdio
            name: 'lasmex-lsp-stdio'
            config:
              servers:
                fixture:
                  command: !!js process.execPath
                  args: ['./lsp-server.mjs']
                  extensionToLanguage:
                    '.ts': typescript
          - id: timeout-policy
            name: 'lasmex-tool-call-timeout-policy'
          - id: tool-lsp
            name: 'lasmex-tool-lsp'
            config:
              maxLocations: 1
          - id: llm-replay
            name: 'lasmex-llm-replay'
