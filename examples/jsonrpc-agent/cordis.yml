# Unattended coding-agent deployment for the bundled lasmex-jsonrpc-agent runtime.
# stdout is reserved for JSON-RPC; do not add a console logger or terminal UI.

- id: sdk-jsonrpc-server
  name: 'lasmex-sdk-jsonrpc-server'
  config:
    maxTokensAsSuccess: !!js "process.env.LASMEX_MAX_TOKENS_AS_SUCCESS === undefined ? true : JSON.parse(process.env.LASMEX_MAX_TOKENS_AS_SUCCESS)"

# The DeepSeek adapter. Shipped default: full thinking at max effort on every
# request; exact-model resolution materializes request defaults before logging.
# The model arrives per session over JSON-RPC, so it is not pinned here.
- id: llm-deepseek
  name: 'lasmex-llm-deepseek'
  config:
    thinking: enabled
    reasoningEffort: max

# Managed child-process groups for the bash executor (spawn/kill/output plumbing).
- id: subprocess
  name: 'lasmex-subprocess-local'

- id: bash
  name: 'lasmex-bash-local'
  config:
    cwd: !!js process.env.LASMEX_CWD ?? process.cwd()
    timeoutMs: 60000

- id: agent-spine
  name: 'lasmex-agent-spine-demo'
  config:
    persona: !!js process.env.LASMEX_SYSTEM_PROMPT ?? 'You are a coding agent.'
    workspaceContext: false
    skills:
      enabled: false
    toolBash:
      enableRunInBackground: false
    toolJobs: false

- id: sessions
  name: 'lasmex-session-persistence-jsonl'
  config:
    root: !!js process.env.LASMEX_SESSION_ROOT ?? './.sessions'
    # Snapshot runs read the raw JSONL back; production keeps zstd frames.
    compression: !!js "process.env.DSH_SNAPSHOT === undefined ? 'zstd' : 'none'"

- id: session-checkpoints
  name: 'lasmex-session-checkpoint-policy'

- id: subagent
  name: 'lasmex-subagent'

- id: subagent-spawn-in-process
  name: 'lasmex-subagent-spawn-in-process'
  config:
    providerName: spawn

- id: tool-subagent
  name: 'lasmex-tool-subagent'
  config:
    provider: spawn
    toolName: subagent
    enableRunInBackground: false

- id: tool-todo
  name: 'lasmex-tool-todo'
  config:
    allowParallelInProgress: true

- id: fs-local
  name: 'lasmex-fs-local'
  config:
    cwd: !!js process.env.LASMEX_CWD ?? process.cwd()

- id: fs-observation-policy
  name: 'lasmex-fs-observation-policy'

- id: tool-fs
  name: 'lasmex-tool-fs'

- id: token-meter
  name: 'lasmex-token-meter'

- id: compaction-basic
  name: 'lasmex-compaction-basic'
  config:
    thresholdRatio: 0.8
    retainRatio: 0.16
    maxTokens: 8192
    compactionRetries: 1
