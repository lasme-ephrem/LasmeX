/**
 * lasmex-base — the shared LasmeX core as a profile bundle. The
 * package's substance is `cordis.patch.yml`, declared by the `lasmex.bundle.patch`
 * manifest field and resolved by the profile composer through that field;
 * this module carries no runtime API.
 * @module lasmex-base
 */

export {}
